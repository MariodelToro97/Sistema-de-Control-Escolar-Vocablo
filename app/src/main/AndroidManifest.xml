<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vocablo.mario.vocablo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- ESCRIBIR ARCHIVOS EN LA MEMORIA EXTERNA -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- LEER ARCHIVOS EN LA SD -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExamenDiagnostico" />
        <activity android:name=".RealizacionDiag" />
        <activity android:name=".RegistrarAlumno" />
        <activity android:name=".RegistrarProfesor" />
        <activity android:name=".RegistrarAdministrativo" />
        <activity android:name=".InterfazAdmin" />
        <activity android:name=".InterfazAlumno" />
        <activity android:name=".InterfazMaestro" />
        <activity android:name=".InterfazPago" />
        <activity android:name=".InterfazProgresoAlumno" />
        <activity android:name=".InterfazAgendarCita" />
        <activity android:name=".InterfazCitasAgendadas"></activity>
    </application>

</manifest>